{
  "name": "elsafeer-pos",
  "private": true,
  "version": "1.0.0",
  "description": "حلواني السفير - نظام نقاط البيع",
  "scripts": {
    "build": "npm --prefix frontend ci --include=dev && npm --prefix frontend run build && npm --prefix backend ci --omit=dev",
    "start": "npm --prefix backend start"
  },
  "engines": {
    "node": "22.x"
  }
}
